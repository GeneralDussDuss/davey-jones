## DAVEY JONES — UI component dependencies.

dependencies:
  idf:
    version: ">=5.3"

  ## esp_lvgl_port brings in LVGL as its own public dependency.
  ## Do NOT also specify lvgl/lvgl here — the component manager will
  ## pull a version that esp_lvgl_port knows how to integrate.
  espressif/esp_lvgl_port:
    version: "^2.4.0"
